# MANSA — Gold Intelligence Platform
# Production requirements — tested with Python 3.11
# ─────────────────────────────────────────────────────────────────────────────

# ── Core web framework ────────────────────────────────────────────────────────
streamlit>=1.35,<2.0

# ── Data processing ───────────────────────────────────────────────────────────
pandas>=2.0,<3.0
numpy>=1.26,<2.0

# ── Visualisation ─────────────────────────────────────────────────────────────
plotly>=5.18,<6.0

# ── Market data (Yahoo Finance fallback — always available) ───────────────────
yfinance>=0.2.38

# ── HTTP (used by Twelve Data + Supabase clients — stdlib urllib, no extra dep)
# requests>=2.31    # uncomment if you prefer requests over urllib

# ── ML prediction models (optional — only needed if using train_models.py) ────
scikit-learn>=1.4
xgboost>=2.0
# tensorflow>=2.15  # uncomment for LSTM model support
# prophet>=1.1      # uncomment for Prophet model support
# joblib>=1.3       # included with scikit-learn

# ── Anthropic AI Advisor ──────────────────────────────────────────────────────
anthropic>=0.25
